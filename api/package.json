{
  "name": "@midnight-pangolin/api",
  "version": "0.1.0",
  "author": "Bett Moses Kiprono",
  "license": "GPL-3.0",
  "private": true,
  "type": "module",
  "module": "./dist/index.js",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.build.json",
    "ci": "npm run typecheck && npm run lint && npm run build",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@midnight-pangolin/contract": "workspace:*"
  }
}
